#!/bin/sh
git config --global user.name “dollar”
git config --global user.email "dollar.wang@emerson.com"
git config --global color.ui true
git config --global alias.st status
git config --global core.editor "vim"
